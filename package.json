{
  "name": "@issimoteam/mojotech-json-type-validation",
  "version": "0.0.3",
  "main": "./dist/index.js",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/issimoteam/json-type-validation"
  },
  "license": "MIT",
  "scripts": {
    "lint": "tslint -t codeFrame --project tsconfig-test.json",
    "clean": "rm -rf ./dist",
    "build": "npm run clean & npx tsc",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "prettier": "^2.4.1",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "fast-deep-equal": "^3.1.3"
  }
}